# JVM per Gradle
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# AndroidX
android.useAndroidX=true

# Stile Kotlin
kotlin.code.style=official

# R class non transitiva (build piu' veloci)
android.nonTransitiveRClass=true
